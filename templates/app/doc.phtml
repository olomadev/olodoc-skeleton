<?php
$this->layout('layout::default');

if (! empty($meta['title'])) {
    $this->headTitle($meta['title']);
    $this->headMeta($meta['description'], "description");
    $this->headMeta($meta['keywords'], "keywords");    
}
$this->placeholder('config')->set($config);
$this->placeholder('version')->set($version);
$this->placeholder('pageJavascript')->set($pageJavascript);
$this->placeholder('documentManager')->set($documentManager);
?>
<!-- CONTENT -->
<section>
  <div class="container-fluid">
    <div class="row">

      <div class="col-12 col-lg-3 col-md-12 col-sm-12 px-lg-0 border-bottom border-bottom-lg-0 border-end-lg border-gray-300 sidenav">
          <!-- Side nav bar begin -->
          <div class="collapse d-lg-block" id="sidenavCollapse">
            <div class="py-md-5 py-lg-5 ps-lg-4 ps-3 pe-4">

              <?php echo $pageVersionCombobox ?>
              <?php echo $pageSearchBoxInput ?>

              <nav class="pagination-nav mt-3">
                <?php echo $pageSidebarHeaderLink ?>
              </nav>

              <ul class="nav flex-column bs-sidenav mt-3">
                <?php echo $pageSideNavbarLinks ?>
              </ul>
            </div>
          </div>
          <!-- Side nav bar end -->
      </div>

      <div id="middle-content" class="document-content col-lg-6 col-md-12 col-sm-12 pt-lg-5 pt-md-3 pt-sm-3">
        <!-- Breadcrumbs -->
        <nav aria-label="breadcrumb" class="main-breadcrumb">
            <?php echo $pageBreadCrumbs ?>    
        </nav>

        <!-- Markdown content -->
        <?php echo $pageContent ?>

        <!-- Pagination Bar -->
        <nav class="pagination-nav mt-5">
          <?php echo $pageFooter ?>
        </nav>
      </div>

      <!-- Sponsors -->
      <div class="document-content col-12 col-lg-2 col-xl-2 ps-4 pt-lg-5 pt-md-3 pt-sm-3">
        <h6 class="text-uppercase">Sponsors</h6>
        <a href="/contact">
          <div class="mb-2" style="border: 2px solid #d6d7da; background-color: #f1f4f8; border-radius: 4px; width:208px;height: 58px;"></div>
          <div style="width:208px;height: 58px;font-size:12px;"><?php echo $this->translate('Your company logo can be here. Contact Us.') ?></div>
        </a>
      </div>

    </div><!-- / .row -->
  </div> <!-- / .container --> 
</section>
